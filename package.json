{
  "name": "slow-down-pacing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Claude Code plugin: configurable work/pause cycles (5 min work / 4 min pause). Silent to the model; safe for the session.",
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\""
  },
  "engines": {
    "node": ">=21"
  }
}